# One-Way Dual-GPU Frame Doubling — Proof Record

Auto-generated by `analyze.py` from the captured evidence in this directory. Every number below is reproducible by re-running the script against the committed logs/stills (no GPU required).

**Rig:** game on RX 9070 XT (`04:00.0`, headless), doubler + display on RX 9060 XT (`87:00.0`, `DP-7` @ 2560×1440). Game = `vkcube` 500×500. `multiplier = 2`.

## 1. Cadence ratio (primary proof)

The app's `-v` stats print, once per second, the received *game* frame rate and the *presented* frame rate. Presented = generated + real, and real = the measured game rate, so the ratio is the observable multiplier.

- stats lines: **36**
- game fps: mean **57.14** (min 56 / max 59)
- presented fps: mean **114.81** (min 112 / max 118)
- **aggregate ratio presented/game = 2.0092**  → multiplier = 2
- cycle count `[gen x 1 + real]` = **2144** (one per captured game frame)
- arithmetic identity: presented − game = **57.7 fps** of generated frames; 2 × cycles = 4288 expected presented

## 2. Engine activity on the doubler card

Mid-run `gpu_busy_percent` (doubler = card3 = RX 9060 XT): baseline **4%** → during run **13–16%** (game card2 = RX 9070 XT ran at 13–16%). The doubler card's engine was measurably busy only while streaming — frame generation ran on it, not on the game GPU.

fdinfo ring-file diffs (binary engine activity trace, pre→post):
- `amd9060_gfx`: 196 of 1025 u64 fields differ pre→post
- `amd9060_sdma0`: 933 of 1025 u64 fields differ pre→post
- `amd9060_sdma1`: 842 of 1025 u64 fields differ pre→post

(`gem_names` is empty even mid-run on this kernel/driver, so `gpu_busy_percent` is the engine-ownership signal.)

## 3. Visual: one fullscreen image (new) vs two cubes (old)

**New code (Run B).** Burst capture cadence ~ 226 ms between frames; whole-frame bright fraction **1.00** (fullscreen content). Consecutive-frame changed-pixel fraction **0.295** mean — a single rotating cube filling the output. Motion bbox covers **66% × 67%** of the frame (one image, not a small window).

**Old code (Run A).** Two-frame pixel-diff found **10 distinct animating regions** >400 px:
- size 29686 px, bbox (1212,780)–(1514,1076) [302×296]
- size 27175 px, bbox (206,248)–(502,562) [296×314]
- size 9478 px, bbox (1248,770)–(1459,846) [211×76]
- size 9246 px, bbox (244,247)–(453,326) [209×79]

Two ~300×300 animating cubes = the game window *and* the old 500×500 overlay both visible = the **ghosting** the user reported. The new code replaces the overlay with a fullscreen scaled blit, so only one image is seen.

## 4. Color fidelity (no channel-swap artifacts)

Motion-pixel isolation removes the static desktop, leaving only the cube. Compared against a standalone `vkcube` capture (no layer) as ground truth:

- standalone vkcube cube: R/G/B (60, 77, 79), blue-dominant 0.371, R−B -19.2
- new code (Run B) cube:    R/G/B (67, 80, 82), blue-dominant 0.292, R−B -15.4
- old code (Run A) cube:    R/G/B (70, 77, 69), blue-dominant 0.187, R−B +1.1

The new code's cube color tracks the ground-truth standalone capture (native B8G8R8A8 format, no R↔B swap). The old code's hardcoded R8G8B8A8 swapchain against the native B8G8R8A8 X visual shifted the signature (green-leaning, R−B near zero) — the **artifacting** the user saw.

## 5. Clean shutdown

- CLEAN: app exited 54 ms after SIGINT (no TERM/KILL fallback)
- `lsfg-vk-app: shutting down` banner present in `app.log`

## Verdict

- Frames **were** doubled by the lossless-scaling algorithm: the presented rate is 2.01× the game rate (1 real + 1 generated per cycle), and the doubler card's engine was busy only while streaming.
- The **ghosting** was two visible cube images (game + old overlay); the new fullscreen scaled blit shows one image.
- The **artifacting** was a channel/format mismatch; the new code's output color matches the true content.

